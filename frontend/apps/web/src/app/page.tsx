import { Button} from '@persofin/shadcn';
import {Check, CheckCircle} from "lucide-react";

export default async function Index() {
  return (
  <div className=""><Button variant={'default'} ><CheckCircle size={15}/>some more test</Button></div>
  );
}

